#![allow(unused)]
struct Solution;

impl Solution {
    fn longest_common_prefix(str: Vec<String>) -> String {
        let mut ans = String::new();
        let temp_str = String::new();
        for name in str {
            //
        }
        return "he".to_string();
    }
}
fn main() {
    let s = vec![
        "flower".to_string(),
        "flow".to_string(),
        "flight".to_string(),
    ];
    println!("ans: {}", Solution::longest_common_prefix(s));
}
